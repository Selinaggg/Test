# 看台 The Stand — 原型实现规格（给 Claude Code）

> 配套文档：同目录 `PRD.md`（读它理解"为什么"，读本文件执行"做什么"）
> 本文件是唯一的实现依据。PRD 与本文件冲突时，以本文件为准。

---

## 0. 一句话任务

做一个**单页、纯前端、无后端**的可交互原型，用左右对比证明一件事：
**大场公屏"看不清、没人接话"，切到看台后"能读完、有人接你"。**

---

## 1. 硬约束

- 技术栈：**原生 HTML + CSS + JavaScript**，单页，不引入任何框架和构建工具
- 产物：`index.html` / `style.css` / `app.js`，双击 index.html 即可运行
- **无后端、无网络请求**，全部数据本地模拟
- 视口：移动端竖屏 **390 × 844**（iPhone 14 尺寸），在桌面浏览器中居中显示为手机外框
- 不做路由、不做登录、不做设置页。**只有一屏。**

---

## 2. 页面结构

```
┌─────────────────────────────┐
│  [模拟直播画面区]            │  上半部分，纯色/渐变占位
│   3.2万人在线 · LIVE         │  角标：在线人数（在 3.1万–3.4万 间小幅波动）
├─────────────────────────────┤
│  公屏评论流（默认态）         │  快速滚动，见 §3
│                             │
│                             │
├─────────────────────────────┤
│  [ 💬 看台 ]   [ 说点什么… ] │  底部操作条
└─────────────────────────────┘
```

点击「看台」按钮 → **底部面板从下方升起**，覆盖屏幕下半部约 60% 高度，可下滑收起。

面板内部结构：
```
┌─────────────────────────────┐
│  ——（拖拽条）——              │
│  [ 全场 ]  [ 看台 ]   换一批  │  tab，默认选中「全场」
├─────────────────────────────┤
│                             │
│      消息流（随 tab 切换）    │
│                             │
├─────────────────────────────┤
│  [ 输入框 ]           [发送] │
└─────────────────────────────┘
```

---

## 3. 两种消息流的差异（这是整个原型的核心，必须做出体感差）

| | 全场 tab | 看台 tab |
|---|---|---|
| 新消息速率 | **每 60–120ms 一条** | **每 5–9 秒一条** |
| 视觉 | 小字号、低对比、快速上滚、**几乎读不完** | 正常字号、气泡样式、清晰、能读完 |
| 头像 | 不显示或极小 | 显示 |
| 内容 | 短、碎、重复、大量"啊啊啊""6666" | 完整句子，是在**接上一句话** |
| 用户发言后 | **淹没**：自己那条 1 秒内被冲出屏幕，无任何回应 | **被接住**：见 §4 |

> 全场流的"快"必须夸张到用户第一眼就觉得"这看不了"。这是说服力的一半。

---

## 4. 关键交互：用户在看台发言后

这是"啊哈时刻"，必须精确实现：

1. 用户在**看台 tab** 输入并发送
2. 自己的消息以高亮气泡插入流中
3. **2–4 秒后**，另一个模拟用户**引用/回应**这条消息
   - 回应文案要显得像真的在接话，用一个通用模板池，例如：
     `"@{我} 说得对，我也这么觉得"` / `"@{我} 哈哈哈这个点确实"` / `"@{我} 不过我觉得还有一种可能…"`
4. **再过 3–6 秒**，第二个人跟上，形成一小段对话
5. 顶部出现一行轻提示：**「有 2 人回应了你」**

对照组：如果用户在**全场 tab** 发言，自己那条消息**1 秒内滚出屏幕，永远没有任何回应**。

> 这个对照是原型唯一要证明的东西。两边都要能操作，让人自己试出差别。

---

## 5. 其他需实现的状态

| 状态 | 行为 |
|---|---|
| 首次切到「看台」tab | 一次性轻提示："已为你分配到 **12 区**，这里有 20 条最近的对话" |
| 进入看台 | 立刻渲染 **20 条历史上下文**（不是空白开场），可向上滚动看到 |
| 点「换一批」 | 消息流淡出 → 换成另一组人和另一段话题 → 提示"已为你换了一批" |
| 面板收起 | 下滑手势或点击拖拽条，回到默认直播态 |
| 面板打开时 | 直播画面区**不被完全遮挡**，仍可见上半部分 |

**不要实现**：点赞、点头、表情、上主屏、排名、主播端、设置、登录、多房间。

---

## 6. 场景设定与模拟语料

### 6.1 场景（钉死在这一分钟，不要泛泛做"一场比赛"）

> **英雄联盟职业联赛 · 季后赛 BO5 第五局 · 34 分钟**
> **蓝方刚被对面偷了大龙，局势可能就此定死**
> 在线 **3.2 万**

为什么选电竞而不是足球：**足球大赛四年一次，做完 demo 等不到验证机会**。电竞联赛赛季内每周都有比赛，是常态化的高人气场景，功能上线后随时能拿真实流量验。

关于人数：**不要用几十万这种夸张数字**。3 万在线是常规赛日头部比赛的常态，也刚好越过 PRD 的启用门槛（>1 万）。

这个数是自洽的：3.2 万在线 × 约 2%/分钟的发言率 ≈ **每分钟 600 余条 ≈ 每秒 10 条**，正好对应 §3 里"每 60–120ms 一条"的全场流速率。**评审若追问速率是否做夸张了，这个换算就是答案。**

**⚠️ 语料黑话红线**：只允许使用大众认知度高的词——**大龙、开团、买活、闪现、下路、打野、辅助、视野、推塔**。禁止出现英雄名缩写、装备缩写、战队梗、圈内黑话。评审席上一定有不玩游戏的人，**一旦他看不懂，注意力就从"方案"跑到"这啥意思"上了。**

### 6.2 全场流语料（短、碎、重复，随机抽取循环）

情绪宣泄型，允许高频重复，重复本身就是真实感：

```
啊啊啊啊啊 / 大龙没了 / 完了 / 这怎么打 / 谁让开的团
离谱 / 6666 / 视野呢 / 打野在干嘛 / 没了没了
还有机会 / 别急 / 稳住 / 我不看了 / 心态崩了
就这？ / 辅助闪现交早了 / 说了别开 / 啊？ / 我靠
这波必输 / 反了反了 / 还能翻 / 兄弟们冷静 / 走了走了
+1 / 太可惜了 / 差一点 / 打得真好 / 对面太稳了
买活啊 / 有人买活吗 / 快回防 / 高地保不住
刚进来发生什么了 / 前面的说下 / 有回放吗 / 比分多少
啊这 / 绝了 / 好家伙 / 血压上来了 / 明年再来
```
至少凑到 60 条，可复用变体。

### 6.3 看台流语料

**关键要求：每一条都必须像在接上一条**，不能是各说各话。这是"看台能讨论起来"的唯一证据。

**话题组 A（默认，复盘这波团该不该开）** —— 20 条上下文：
```
1  这波团其实不该开，视野是空的
2  但不开的话大龙也保不住啊
3  保不住可以放，血量还够守高地
4  放了下一波就更难打了，装备差距会拉开
5  问题是他们进场的时候我方辅助还没到位
6  辅助那时候在下路补线，回不来
7  那就是节奏没对齐，不是这波打不了
8  对，是前面分路的时候就已经错开了
9  其实30分钟那波推塔就该收一收
10 那波推得确实有点贪
11 贪也正常，当时有人头优势
12 优势期打得急一点没毛病，主要是视野没跟上
13 视野是真的空，整个大龙区一个眼都没有
14 眼位是辅助的活，但他一直在下路
15 所以还是分工的问题
16 现在说这些也晚了，看能不能守住高地
17 守高地要看买活，还有两个能买
18 买活够的话还能拖
19 拖到后期装备成型确实有机会
20 就看下一波谁先犯错了
```
之后持续生成的新消息也要沿着这条线往下走。

**话题组 B（「换一批」后，BP 与阵容视角）** —— 另起 20 条，讨论方向完全不同：
```
其实这局 BP 就已经不太对了
对面拿的阵容更适合打后期
我方是想打节奏的，但节奏没打出来
前期没滚起来，中期就很难受
第三手那个选择挺冒险的
冒险但合理，赌的就是前期
赌输了就是现在这样
...
```
同样凑够 20 条 + 生成池，**人和话题都要和 A 组明显不同**，让「换一批」有实感。

### 6.4 用户发言后的回应语料

回应必须**引用用户原话的关键词**，不能是通用敷衍。模板池：
```
@{我} 同意，我也是这么看的
@{我} 这个点确实关键
@{我} 不过我觉得还得看当时的血量
@{我} 你这么一说我又去看了眼回放
@{我} 有道理，前面没人提这个
```

### 6.5 其他
- 昵称用中文网名风格，头像用首字母色块占位
- 全部数据写在 `app.js` 顶部的常量区，便于替换

---

## 7. 验收标准（做完自查）

- [ ] 打开页面，第一眼就觉得全场评论"快到没法读"
- [ ] 点「看台」→ 面板升起流畅，能看到 20 条上下文而不是空白
- [ ] 在看台发一句话，**5 秒内有人回你**，10 秒内形成 3 条对话
- [ ] 切回全场发一句话，**它被冲走且无人回应**
- [ ] 「换一批」能换到明显不同的一组人和话题
- [ ] 面板可下滑收起，收起后直播画面完整
- [ ] 全程无报错，无网络请求

---

## 8. 视觉基调

- 深色背景（直播间语境），主色克制
- 看台面板用**略亮的表面色**，与全场的"混乱感"形成对比
- 动效只做两处：面板升起/收起、消息进入。其余不要动效。
- 不要做炫技的粒子、渐变光效——**原型要显得像真产品，不像 demo**
